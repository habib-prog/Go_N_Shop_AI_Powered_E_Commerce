export default function App() {
  return (
    <main className="app-shell">
      <section className="hero-card">
        <p className="eyebrow">Go N Shop</p>
        <h1>React frontend is ready inside the same repo.</h1>
        <p className="copy">
          This workspace now has a backend at the root and a frontend starter
          in <code>apps/web</code>.
        </p>
      </section>
    </main>
  );
}
