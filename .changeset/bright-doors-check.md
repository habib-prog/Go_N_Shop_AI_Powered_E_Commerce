---
"go_n_shop": patch
---

Split auth logic into use-case focused service files and separate auth controllers for signup and login flows, while removing the legacy `authService` compatibility index.
